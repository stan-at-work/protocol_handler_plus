name: protocol_handler_plus_platform_interface
description: A common platform interface for the protocol_handler_plus plugin.
version: 1.0.0
homepage: https://github.com/stan-at-work/protocol_handler_plus/blob/main/packages/protocol_handler_plus_platform_interface

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  
